# スマホだけでセットアップする手順書

このファイルは **パソコンを使わず、スマートフォンだけ** で
E資格の学習Webアプリを動かせるようにするための手順書です。

エンジニアでない人が読む前提で書いています。
専門用語が出てきたら、必ず「ふだんの言葉での意味」を先に書いています。

---

## 0. まず全体像（3分でわかる説明）

### いま起きること

```
   あなたのスマホ
        │
        │ ① GitHubにコードを置く
        ▼
   GitHub（コードの保管庫）
        │
        │ ② mainブランチが更新されたら自動で
        ▼
   GitHub Actions（自動作業ロボット）
        │
        │ ③ clasp というツールで送る
        ▼
   Google Apps Script（アプリの裏側）
        │
        │ ④ WebアプリのURLが自動で新しくなる
        ▼
   スマホのホーム画面のアイコン（あなたが使う画面）
```

### 用語の意味

| 出てくる言葉 | ふだんの言葉での意味 | 正式名称 |
|---|---|---|
| リポジトリ | コードを入れておく箱 | Repository |
| プライベート | 自分だけが見られる設定 | Private |
| ブランチ | 作業用の枝分かれ。`main` が本番 | Branch |
| Codespaces | ブラウザの中で動く作業用パソコン | GitHub Codespaces |
| ターミナル | 文字で命令を打ち込む黒い画面 | Terminal |
| Secrets | GitHubの中にある鍵の保管庫 | GitHub Secrets |
| Actions | 自動でお使いをしてくれるロボット | GitHub Actions |
| clasp | Apps Scriptへコードを送る道具 | Command Line Apps Script |
| デプロイ | 「公開する」「反映する」こと | Deploy |
| バージョン（版） | 巻き戻せるようにした保存ポイント | Version |

### 大事な区切り

| | 初回だけ | 2回目以降 |
|---|---|---|
| Googleへのログイン | **必要（あなた本人）** | 不要 |
| Apps Script API のオン | **必要（あなた本人）** | 不要 |
| GitHub Secretsの登録 | **必要（1回だけ）** | 不要 |
| 最初のWebアプリ公開 | **必要（スクリプトが手伝います）** | 不要 |
| コードをApps Scriptへ反映 | スクリプトが実行 | **完全に自動** |

つまり、**この手順書を最後までやるのは1回だけ**です。
そのあとは、GitHubの `main` が更新されるたびに自動で反映されます。

---

## 1. GitHubのリポジトリをプライベートにする

学習データそのものは入っていませんが、
スプレッドシートのIDなどが入っているため、**必ずプライベート**にしてください。

### 操作

1. スマホのブラウザで `https://github.com` を開く
2. 自分のアカウントでログインする
3. リポジトリ `e-shikaku-study-app` を開く
4. 上部のタブから **Settings**（歯車のマーク）を押す
   - 画面が狭くてタブが見えないときは、リポジトリ名の下を横にスワイプする
5. 一番下までスクロールする
6. **Danger Zone** の中の **Change repository visibility** を押す
7. **Make private** を選ぶ
8. 確認のためリポジトリ名を入力して確定する

> すでにプライベートなら、この章は飛ばしてOKです。

---

## 2. Claude Code on the web でコードを準備する（済んでいる場合は飛ばす）

**Claude Code on the web** とは、ブラウザ上でAIにコードを直してもらう仕組みです。

このリポジトリはすでに自動デプロイ用の形になっているので、
通常は何もしなくて大丈夫です。

あとから直したいことが出てきたときは、

1. `https://claude.ai/code` を開く
2. リポジトリ `e-shikaku-study-app` を選ぶ
3. やりたいことを日本語で伝える
4. 作業用ブランチができるので、内容を確認して `main` へ取り込む（→ 第9章）

という流れになります。

---

## 3. GitHub Codespaces を起動する

**Codespaces** は「ブラウザの中で動く作業用のパソコン」です。
あなたのスマホには何もインストールされません。使い終われば消せます。

### 操作

1. スマホのブラウザでリポジトリ `e-shikaku-study-app` を開く
2. 緑色の **`<> Code`** ボタンを押す
3. 出てきた画面で **Codespaces** タブを選ぶ
4. **Create codespace on main** を押す
   - 2回目以降は、すでにあるCodespaceの名前を押せば再開できます
5. 「Setting up your codespace」と出るので、そのまま待つ（初回は2〜4分）
6. 黒っぽい編集画面（VS Code）が開いたら成功

> **ヒント**: スマホは画面が狭いので、横向きにすると操作しやすくなります。

### ターミナル（黒い画面）を開く

1. 画面の左上にある **≡**（三本線）を押す
2. **Terminal** → **New Terminal** を選ぶ
3. 画面の下側に文字を打ち込める場所が出れば成功

---

## 4. セットアップのコマンドを1つだけ実行する

ターミナルに、次の1行を **そのまま** 打ち込んで、Enterを押してください。

```bash
bash scripts/bootstrap-smartphone.sh
```

> コピー＆ペーストが楽です。長押し → ペースト。

このあとは、画面の指示にそって進めます。
途中で止まってしまっても、**同じコマンドをもう一度実行すれば続きから再開**できます。

このスクリプトが自動でやってくれること:

- 環境の確認（Node.js 20以上など）
- 必要な部品のインストール
- コードの検査（壊れていないか、秘密が混ざっていないか）
- Apps Scriptプロジェクトの作成または接続
- Apps Scriptへの反映
- 版（バージョン）の作成
- **初回のWebアプリ公開**
- GitHub Secretsの登録の手伝い
- 最後にWebアプリURLを表示

---

## 5. Googleへログインする（あなた本人の操作）

スクリプトが進むと、Googleログインの場面になります。

> ここは**自動化してはいけない部分**です。
> あなたのGoogleアカウントとスプレッドシートを守るため、
> ログインと許可は必ずあなた自身が行います。
> スクリプトはパスワードを見ることも、保存することもありません。

### 画面の流れ

1. ターミナルに **とても長いURL** が表示される
2. そのURLを **長押し** して「リンクを開く」または「コピー」する
3. ブラウザで開く
4. **E資格のスプレッドシートを持っているGoogleアカウント** を選ぶ
   - ここを間違えると、スプレッドシートが見つからずアプリが動きません
5. 「このアプリが次の権限を求めています」と出るので内容を確認する
   - スプレッドシートの参照と編集
   - Googleドライブの読み取り（画像ZIPを読むため）
6. **続行 / 許可** を押す
7. 画面に **コード** が表示されるのでコピーする
8. Codespacesのターミナルへ戻って貼り付け、Enterを押す

> 「`--no-localhost`」という表示が出ますが、これは
> 「スマホでも認証できるやり方を使います」という意味です。そのまま進めてOKです。

---

## 6. Apps Script API をオンにする（初回だけ）

**Apps Script API** とは、
「GitHubからApps Scriptを書き換えてよい」ための入口のスイッチです。

スクリプトが自動で確認します。まだオフなら、次の案内が出ます。

### 操作

1. ブラウザで `https://script.google.com/home/usersettings` を開く
2. **Google Apps Script API** という項目を探す
3. スイッチを **オン** にする
4. Codespacesのターミナルへ戻って **Enter** を押す

> オンにした直後は反映に少し時間がかかることがあります。
> 「まだ有効になっていません」と言われたら、30秒ほど待ってから
> `bash scripts/bootstrap-smartphone.sh` をもう一度実行してください。

---

## 7. Apps Scriptプロジェクトを決める

スクリプトが `A` か `B` を聞いてきます。

- **A) 新しく作る（おすすめ）**
  いま動いているアプリを壊しません。新しいURLで確認してから乗り換えられます。
- **B) 既存につなぐ**
  すでにApps Scriptプロジェクトがあって、そのURLをそのまま更新したい場合。
  Apps Scriptの画面URLの `https://script.google.com/d/` と `/edit` の間の
  長い文字列（スクリプトID）を貼り付けます。

迷ったら **A** を選んでください。

---

## 8. GitHub Secrets を登録する（初回だけ）

**GitHub Secrets** は「GitHubの中の鍵の保管庫」です。
ここに入れた値は、コードにも実行ログにも表示されません。

登録するもの:

| 名前 | 中身 | ふだんの言葉で言うと |
|---|---|---|
| `CLASPRC_JSON` | Googleのログイン情報 | あなたの鍵 |
| `CLASP_JSON` | どのApps Scriptへ送るかの情報 | 送り先の住所 |
| `DEPLOYMENT_ID` | 更新するWebアプリの番号 | 更新する部屋の番号 |

### 方法A: スクリプトに任せる（かんたん）

スクリプトが「GitHub CLI を使って自動登録しますか？」と聞いてきます。
`y` と入力してEnterを押せば、自動で登録されます。

権限が足りずに失敗した場合は、ターミナルで次を実行してから、
もう一度 `bash scripts/bootstrap-smartphone.sh` を実行してください。

```bash
gh auth refresh -h github.com -s repo
```

### 方法B: 手で登録する

1. ターミナルで中身を表示する

   ```bash
   cat ~/.clasprc.json
   ```

   表示された文字を **すべて** 長押しでコピーする

2. ブラウザでリポジトリを開く
3. **Settings** → 左メニューの **Secrets and variables** → **Actions**
4. 緑の **New repository secret** を押す
5. **Name** に `CLASPRC_JSON` と入力
6. **Secret** に、さきほどコピーした内容を貼り付ける
7. **Add secret** を押す
8. 同じ手順で、次の2つも登録する

   ```bash
   cat .clasp.json
   ```
   → `CLASP_JSON`

   スクリプトが表示した `DEPLOYMENT_ID = ...` の値
   → `DEPLOYMENT_ID`

> **注意**
> - 貼り付けた画面のスクリーンショットを保存しないでください。
> - これらの値をチャットやメールへ送らないでください。
> - Secretsに入れた値は、あとから見ることはできません（上書きだけ可能）。それが正常です。

---

## 9. WebアプリのURLを確認して、ホーム画面へ追加する

### URLの確認

スクリプトの最後に、次のような行が表示されます。

```
あなたのWebアプリURL:
  https://script.google.com/macros/s/AKfycb.../exec
```

これがあなた専用の学習アプリのURLです。

見失った場合は、Apps Scriptの編集画面
（`https://script.google.com/d/<スクリプトID>/edit`）を開き、
右上の **デプロイ** → **デプロイを管理** から確認できます。

### 最初の1回だけ出る許可画面

URLを開くと「承認が必要です」と出ることがあります。
自分が作ったアプリなので、内容を確認して **許可** を押してください。

### ホーム画面へ追加する

**iPhone (Safari)**

1. URLを開く
2. 下の **共有ボタン**（□に↑）を押す
3. **ホーム画面に追加** を選ぶ
4. 名前を「E資格」などにして **追加**

**Android (Chrome)**

1. URLを開く
2. 右上の **⋮**（縦三点）を押す
3. **ホーム画面に追加** を選ぶ
4. **追加** を押す

これでアプリのアイコンから直接開けます。
URLは今後も変わらないので、アイコンは作り直さなくて大丈夫です。

---

## 10. 2回目以降：自動デプロイの流れ

ここから先は、**あなたは何も設定しなくてよくなります**。

```
   コードを直す（Claude Code on the web / Codespaces / GitHub上で編集）
        │
        ▼
   main ブランチへ入れる（Pull Requestをマージ、など）
        │
        ▼
   GitHub Actions が自動で動く
        │
        ├─ ① 検査（CIと同じ）
        │     ・必須ファイルがそろっているか
        │     ・JavaScriptが壊れていないか
        │     ・秘密の情報が混ざっていないか
        │     ・理解度v2 / 画像必須 / 正解漏洩防止のルールが守られているか
        │
        │   ✗ 失敗 → ここで止まる（Apps Scriptには何も送られない）
        │   ✓ 成功 → 次へ
        │
        ├─ ② clasp push（Apps Scriptへ送る）
        ├─ ③ 版を作る（あとで巻き戻せる）
        └─ ④ 既存のWebアプリを新しい版へ切り替える
        │
        ▼
   スマホのホーム画面のアイコンから開くと、もう新しくなっている
```

### 動いたか確認する方法

1. リポジトリの **Actions** タブを開く
2. 一番上の実行を押す
3. 緑のチェック ✓ なら成功、赤い × なら失敗

赤い × のときは、Apps Scriptには**何も送られていません**。
今まで動いていたアプリはそのまま使えます。

---

## 11. 困ったときは

| 症状 | 原因として多いもの | 対処 |
|---|---|---|
| `Node.js 20以上が必要です` | 古いCodespaceを再利用している | Codespaceを削除して作り直す |
| `Apps Script API がまだ有効でない` | スイッチがオフ／反映待ち | 第6章をやり直し、30秒待って再実行 |
| `No credentials found` | Googleログインが切れた | `npx clasp login --no-localhost` を実行 |
| Actionsが赤くなる | Secretsが未登録／期限切れ | 第8章をやり直す |
| アプリが「権限がありません」と出る | 別のGoogleアカウントでログインした | スプレッドシートの持ち主のアカウントで開く |
| 画像問題が出てこない | 画像を取得できない問題は出題しない設計 | 仕様どおりです。しばらくすると再度候補に戻ります |

### Googleのログイン情報を入れ直したいとき

Codespacesのターミナルで:

```bash
npx clasp login --no-localhost
gh secret set CLASPRC_JSON < ~/.clasprc.json
```

---

## 12. やってはいけないこと

- `.clasprc.json` や `.clasp.json` の中身をコードとしてコミットする
  （`.gitignore` で防いでいますが、無理に追加しないでください）
- Secretsの中身をチャット・メール・スクリーンショットで共有する
- スプレッドシートの数式（特に `02_マインドマップ` のM列、`04_学習ログ` のU列）を直接書き換える
- 学習データ（回答ログ）をGitHubへコピーする
- テストを消して赤いまま `main` へ入れる

---

## 付録: よく使うコマンド

Codespacesのターミナルで使えます。

```bash
# 初回セットアップ（何度実行しても安全）
bash scripts/bootstrap-smartphone.sh

# 検査だけ実行する（Apps Scriptへは送りません）
npm run verify

# 手動でApps Scriptへ反映する
npm run deploy

# いまどのGoogleアカウントでログインしているか
npx clasp show-authorized-user
```
